services:
  cat-food-monitor:
    build: .
    container_name: cat-food-monitor
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - baseline-data:/app/data
    dns:
      - ${DNS_SERVER:-8.8.8.8}

volumes:
  baseline-data:
